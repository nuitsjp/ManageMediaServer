# 家庭内 LAN の CIDR に置き換える。
# 現在の家庭内 LAN は 192.168.0.0/24。
LAN_CIDR=192.168.0.0/24

# Tailscale のインターフェース名。通常は tailscale0。
TAILSCALE_IFACE=tailscale0

# Docker published port の到達元制限対象。
MEDIA_FIREWALL_PORTS="2283 8096"
